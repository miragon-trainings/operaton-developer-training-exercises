{
  "name": "operaton-developer-training-exercises-tooling",
  "version": "1.0.0",
  "private": true,
  "description": "Dev tooling for the Operaton training exercises: BPMN linting (bpmnlint:recommended, configured via the root .bpmnlintrc).",
  "scripts": {
    "lint:bpmn": "bpmnlint \"services/**/src/main/resources/bpmn/*.bpmn\""
  },
  "devDependencies": {
    "bpmnlint": "11.12.1"
  }
}
