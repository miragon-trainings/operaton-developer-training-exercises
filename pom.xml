<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
         https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <!-- Aligned to the Spring Boot version Operaton 2.1.3 is built and tested against.
             Boot 4.1.0 drops the auto-configured RestClient.Builder bean the adapters rely on. -->
        <version>4.0.7</version>
        <relativePath/>
    </parent>

    <groupId>io.miragon</groupId>
    <artifactId>operaton-developer-training-exercises</artifactId>
    <version>1.0-SNAPSHOT</version>
    <packaging>pom</packaging>
    <name>operaton-developer-training-exercises</name>

    <!--
      Nur der Hauptservice, an dem Teilnehmende von Anfang an arbeiten, plus die Referenzlösungen.
      Der Aufgabe-10-Worker (logistics-service) taucht bewusst NICHT hier auf – er wird erst in Aufgabe 10
      aus templates/exercise-10/ heraus angelegt. So bleibt services/ für die Aufgaben 0–9 aufgeräumt.
      (Zum Bauen/Prüfen der Vorlage: Profil -Pexercise-10, siehe unten.)
    -->
    <modules>
        <module>services/process-application</module>
        <module>solutions</module>
    </modules>

    <properties>
        <java.version>21</java.version>
        <postgresql.version>42.7.8</postgresql.version>
        <operaton.version>2.1.3</operaton.version>
        <archunit.version>1.5.0</archunit.version>
        <bpmn-to-code.version>5.2.0</bpmn-to-code.version>
        <openapi-generator.version>7.24.0</openapi-generator.version>
    </properties>

    <!--
      Nur Versions-Verwaltung – erzwingt KEINE Dependencies. Jedes Modul wählt selbst,
      welche Deps es zieht (ohne Version); Spring-Boot-Deps managt der Parent.
    -->
    <dependencyManagement>
        <dependencies>
            <!-- Operaton (direct – no process-engine-api abstraction) -->
            <dependency>
                <groupId>org.operaton.bpm.springboot</groupId>
                <artifactId>operaton-bpm-spring-boot-starter-webapp</artifactId>
                <version>${operaton.version}</version>
            </dependency>
            <dependency>
                <groupId>org.operaton.bpm.springboot</groupId>
                <artifactId>operaton-bpm-spring-boot-starter-rest</artifactId>
                <version>${operaton.version}</version>
            </dependency>
            <!-- External Task Client (remote worker connecting to the engine REST API, ab Aufgabe 10) -->
            <dependency>
                <groupId>org.operaton.bpm.springboot</groupId>
                <artifactId>operaton-bpm-spring-boot-starter-external-task-client</artifactId>
                <version>${operaton.version}</version>
            </dependency>
            <!-- Architecture tests -->
            <dependency>
                <groupId>com.tngtech.archunit</groupId>
                <artifactId>archunit-junit5</artifactId>
                <version>${archunit.version}</version>
            </dependency>
            <!-- Process tests: Operaton port of camunda-bpm-assert (BpmnAwareTests) -->
            <dependency>
                <groupId>org.operaton.bpm</groupId>
                <artifactId>operaton-bpm-assert</artifactId>
                <version>${operaton.version}</version>
                <scope>test</scope>
            </dependency>
            <!-- Engine core — a worker without an engine starter (Aufgabe 10) brings it explicitly
                 to spin up a standalone in-memory engine for its own process tests -->
            <dependency>
                <groupId>org.operaton.bpm</groupId>
                <artifactId>operaton-engine</artifactId>
                <version>${operaton.version}</version>
            </dependency>
            <!-- Runtime types referenced by the bpmn-to-code generated process API -->
            <dependency>
                <groupId>io.miragon</groupId>
                <artifactId>bpmn-to-code-runtime</artifactId>
                <version>${bpmn-to-code.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <pluginManagement>
            <plugins>
                <!-- Spring Boot fat-jar packaging – declared here so child modules can reference it without version -->
                <plugin>
                    <groupId>org.springframework.boot</groupId>
                    <artifactId>spring-boot-maven-plugin</artifactId>
                    <configuration>
                        <excludes>
                            <exclude>
                                <groupId>org.springframework.boot</groupId>
                                <artifactId>spring-boot-devtools</artifactId>
                            </exclude>
                        </excludes>
                    </configuration>
                </plugin>
                <!-- bpmn-to-code: generates the type-safe process API constants from the BPMN models.
                     Declared here so child modules (ab Aufgabe 5) can reference it without version. -->
                <plugin>
                    <groupId>io.miragon</groupId>
                    <artifactId>bpmn-to-code-maven</artifactId>
                    <version>${bpmn-to-code.version}</version>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>

    <profiles>
        <!--
          Aufgabe 10 baut auf einem zweiten Service (dem logistics-service) auf, der erst dort angelegt
          wird. Die Vorlage liegt unter templates/exercise-10/ und ist NICHT Teil des Standard-Builds.
          Mit diesem Profil lässt sich die Vorlage bauen/prüfen (CI nutzt es genau dafür):
              ./mvnw -Pexercise-10 verify
        -->
        <profile>
            <id>exercise-10</id>
            <modules>
                <module>templates/exercise-10/logistics-service</module>
            </modules>
        </profile>
    </profiles>

</project>
