server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/operaton-training?currentSchema=exercise
    username: admin
    password: admin
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        default_schema: exercise

operaton:
  bpm:
    admin-user:
      id: admin
      password: admin
    database:
      type: postgres
      schema-update: true

# In-Engine "Notify community": announce new members in a Microsoft Teams channel via a Power
# Automate "Workflows" webhook. Ab Aufgabe 9 wird dieser Aufruf in einen eigenen Worker-Service
# ausgelagert (Wiederverwendung + Isolation des Secrets).
notification:
  teams:
    # Supply the real webhook URL via the TEAMS_WEBHOOK_URL env var — do NOT commit the secret.
    webhook-url: ${TEAMS_WEBHOOK_URL:https://CHANGE-ME}
