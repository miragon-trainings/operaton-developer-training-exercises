<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
         https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>io.miragon</groupId>
        <artifactId>solutions</artifactId>
        <version>1.0-SNAPSHOT</version>
    </parent>

    <artifactId>solution-extra-task-1</artifactId>
    <name>solution-extra-task-1</name>

    <properties>
        <!-- bpm-crafters process-engine-api: engine-neutral abstraction layer -->
        <process-engine-api.version>1.7</process-engine-api.version>
        <process-engine-worker.version>0.8.5</process-engine-worker.version>
        <!--
          The bpm-crafters Operaton adapter is not yet released to Maven Central (see the note in
          solutions/pom.xml). Until it ships, use the develop SNAPSHOT (resolved from the Central
          snapshots repository declared below). Bump to the release version once available.
        -->
        <process-engine-adapter-operaton.version>2026.07.2-SNAPSHOT</process-engine-adapter-operaton.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <!-- Aligns the versions of the Operaton process-engine-adapter modules -->
            <dependency>
                <groupId>dev.bpm-crafters.process-engine-adapters</groupId>
                <artifactId>process-engine-adapter-operaton-bom</artifactId>
                <version>${process-engine-adapter-operaton.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-devtools</artifactId>
            <optional>true</optional>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>
        <dependency>
            <groupId>org.postgresql</groupId>
            <artifactId>postgresql</artifactId>
        </dependency>
        <dependency>
            <groupId>com.h2database</groupId>
            <artifactId>h2</artifactId>
            <scope>runtime</scope>
        </dependency>
        <!-- Operaton engine + Cockpit-Webapp + REST-API (Version zentral gemanagt) -->
        <dependency>
            <groupId>org.operaton.bpm.springboot</groupId>
            <artifactId>operaton-bpm-spring-boot-starter-webapp</artifactId>
        </dependency>
        <dependency>
            <groupId>org.operaton.bpm.springboot</groupId>
            <artifactId>operaton-bpm-spring-boot-starter-rest</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>com.tngtech.archunit</groupId>
            <artifactId>archunit-junit5</artifactId>
            <scope>test</scope>
        </dependency>
        <!-- bpm-crafters process-engine-api layer -->
        <!-- Engine-neutral process orchestration API (start, correlate, workers) -->
        <dependency>
            <groupId>dev.bpm-crafters.process-engine-api</groupId>
            <artifactId>process-engine-api</artifactId>
            <version>${process-engine-api.version}</version>
        </dependency>
        <!-- @ProcessEngineWorker support (replaces JavaDelegate) -->
        <dependency>
            <groupId>dev.bpm-crafters.process-engine-worker</groupId>
            <artifactId>process-engine-worker-spring-boot-starter</artifactId>
            <version>${process-engine-worker.version}</version>
        </dependency>
        <!-- Operaton adapter: plugs the process-engine-api onto the embedded Operaton engine -->
        <dependency>
            <groupId>dev.bpm-crafters.process-engine-adapters</groupId>
            <artifactId>process-engine-adapter-operaton-embedded-spring-boot-starter</artifactId>
            <version>${process-engine-adapter-operaton.version}</version>
        </dependency>
        <!-- Runtime types referenced by the bpmn-to-code generated process API -->
        <dependency>
            <groupId>io.miragon</groupId>
            <artifactId>bpmn-to-code-runtime</artifactId>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <!-- bpmn-to-code: generates the type-safe process API constants from the BPMN models -->
            <plugin>
                <groupId>io.miragon</groupId>
                <artifactId>bpmn-to-code-maven</artifactId>
                <executions>
                    <execution>
                        <id>generate-process-api</id>
                        <phase>generate-sources</phase>
                        <goals>
                            <goal>generate-bpmn-api</goal>
                        </goals>
                    </execution>
                </executions>
                <configuration>
                    <baseDir>${project.basedir}</baseDir>
                    <filePattern>src/main/resources/bpmn/*.bpmn</filePattern>
                    <outputFolderPath>${project.basedir}/src/main/java</outputFolderPath>
                    <packagePath>io.miragon.training.adapter.process</packagePath>
                    <outputLanguage>JAVA</outputLanguage>
                    <processEngine>CAMUNDA_7</processEngine>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <mainClass>io.miragon.training.TrainingApplication</mainClass>
                </configuration>
            </plugin>
        </plugins>
    </build>

    <!--
      Only needed while the bpm-crafters Operaton adapter is a SNAPSHOT (see the version note above).
      Remove once a released adapter version is used.
    -->
    <repositories>
        <repository>
            <id>central-snapshots</id>
            <name>Maven Central Snapshots</name>
            <url>https://central.sonatype.com/repository/maven-snapshots/</url>
            <releases>
                <enabled>false</enabled>
            </releases>
            <snapshots>
                <enabled>true</enabled>
            </snapshots>
        </repository>
    </repositories>

</project>
