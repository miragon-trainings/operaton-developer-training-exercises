package io.miragon.training.adapter.inbound.operaton;

// TODO Exercise 4: Uncomment this class (remove the /* and */ lines)
//  and then implement the TODO in executeTask(...).
//  It needs the Operaton engine that you switch on in Exercise 1.
/*
import io.miragon.training.application.port.inbound.SendConfirmationMailUseCase;
import io.miragon.training.domain.MembershipId;
import org.operaton.bpm.engine.delegate.DelegateExecution;
import org.springframework.stereotype.Component;

import java.util.UUID;

@Component
public class SendConfirmationMailDelegate extends BaseDelegate {

    private final SendConfirmationMailUseCase useCase;

    public SendConfirmationMailDelegate(SendConfirmationMailUseCase useCase) {
        this.useCase = useCase;
    }

    @Override
    protected void executeTask(DelegateExecution execution) {
        // TODO Exercise 4: Implement this delegate's engine binding yourself.
        //  - Read the process variable "membershipId" from the DelegateExecution (getVariable).
        //  - Convert the value into a MembershipId and call useCase.sendConfirmationMail(...) with it.
        //  How you obtain the variable via the DelegateExecution and convert the String is something
        //  you look up yourself – there is deliberately no ready-made call line here.
        throw new UnsupportedOperationException("Exercise 4: read the 'membershipId' variable and call the use case");
    }
}
*/
