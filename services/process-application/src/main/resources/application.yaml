# TODO Exercise 1: Uncomment the configuration.
#
# It connects the engine to the PostgreSQL from stack/ (docker-compose) and creates the
# Cockpit admin user. Without this configuration the engine finds no database and the
# application does not start.
#
# The engine runs on port 8080 with its own schema `exercise`.

server:
  port: 8080

#spring:
#  datasource:
#    url: jdbc:postgresql://localhost:5432/operaton-training?currentSchema=exercise
#    username: admin
#    password: admin
#    driver-class-name: org.postgresql.Driver
#  jpa:
#    hibernate:
#      ddl-auto: create
#    properties:
#      hibernate:
#        dialect: org.hibernate.dialect.PostgreSQLDialect
#        default_schema: exercise
#
#operaton:
#  bpm:
#    admin-user:
#      id: admin
#      password: admin
#    database:
#      type: postgres
#      schema-update: true
